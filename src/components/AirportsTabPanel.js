//Renders information about airports in the picked country

import { Paper, Typography } from "@mui/material";

const AirportsTabPanel = () => {
  return (
    <Paper sx={{ padding: 2 }}>
      <Typography variant="h4">Airports</Typography>
    </Paper>
  );
};

export default AirportsTabPanel;
